# Adobe Video Hub Demo (Lit + TypeScript)

This demo implements a multi-section marketing + interactive hub for Adobe video features.
It uses Lit and Vite. Replace sample media links with your own assets.

## Run locally

1. npm install
2. npm run dev
3. Open http://localhost:5173
